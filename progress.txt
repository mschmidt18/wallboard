Issue #1 - Photos config key mismatch: `interval` vs `interval_seconds` - DONE (commit 31ced8c)
  - Fixed PhotosConfig.tsx to emit `interval_seconds` instead of `interval`
  - Renamed local state from `interval`/`setInterval_` to `intervalSeconds`/`setIntervalSeconds`
  - Updated emitChange type and body to use `interval_seconds`
  - Added backend contract test `test_photos_widget_config_uses_interval_seconds`
  - All 67 backend tests pass

Issue #2 - Clock config key mismatch: `use_24h` vs `format_24h` - DONE
  - Fixed ClockConfig.tsx to emit `format_24h` instead of `use_24h`
  - Renamed local state from `use24h`/`setUse24h` to `format24h`/`setFormat24h`
  - Updated handleChange type and body to use `format_24h`
  - Added backend contract test `test_clock_widget_config_uses_format_24h`
  - All 68 backend tests pass

Issue #3 - No Google OAuth token refresh - DONE (commit f2ce13c)
  - Added `get_valid_access_token()` in `google_auth.py` that checks `expires_at` and refreshes via `refresh_access_token()` when expired
  - Saves refreshed tokens (access_token, expires_at, preserves refresh_token) back to the Integration DB row
  - Returns None with warning log when no refresh_token is available
  - Updated `_get_google_access_token` in `refresh.py` to use `get_valid_access_token` (now async)
  - Updated `_get_access_token` in `google_data.py` to use `get_valid_access_token` (now async)
  - Updated `google_callback` in `integrations.py` to store `expires_at` during initial OAuth code exchange
  - Added 5 new tests in `test_token_refresh.py`: valid token, expired token refresh, no refresh token, no integration, missing expires_at
  - Updated `test_google_data.py` to mock `_get_access_token` directly instead of `load_or_create_key`
  - All 73 backend tests pass

Issue #4 - Calendar widget deduplication silently drops differing configs - DONE
  - Changed cache key from static `google_calendar` to parameterized `google_calendar_{sorted_ids}_{days_ahead}`
  - Updated `_collect_data_sources` in `refresh.py` to generate unique keys per calendar config
  - Updated `_get_cache_key` in `display.py` to match the new key format
  - Added test `test_collect_data_sources_calendar_different_configs` verifying two calendar widgets with different configs produce separate data source entries
  - All 74 backend tests pass

Issue #5 - Google Photos/Albums API pagination not handled - DONE
  - Added pagination loop in `fetch_albums` to follow `nextPageToken` via query params
  - Added pagination loop in `fetch_album_photos` to follow `nextPageToken` via request body
  - Both functions now accumulate results across all pages before returning
  - Added test `test_fetch_albums_paginates` verifying two pages of albums are combined
  - Added test `test_fetch_album_photos_paginates` verifying two pages of photos are combined
  - All 76 backend tests pass

Issue #6 - Theme settings not applied to dashboard display - DONE
  - Added theme CSS mapping constants in Dashboard.tsx for font_family, font_scale, and widget_background
  - Applied `text_color` (light/dark) as CSS color on root dashboard element
  - Applied `font_family` as CSS font-family on root dashboard element
  - Applied `font_scale` as CSS font-size (rem) on root dashboard element
  - Applied `widget_background` (transparent/semi-transparent/solid) as backgroundColor on each widget cell
  - No lint errors in Dashboard.tsx; all 76 backend tests pass
