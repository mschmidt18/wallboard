Issue #1 - Photos config key mismatch: `interval` vs `interval_seconds` - DONE (commit 31ced8c)
  - Fixed PhotosConfig.tsx to emit `interval_seconds` instead of `interval`
  - Renamed local state from `interval`/`setInterval_` to `intervalSeconds`/`setIntervalSeconds`
  - Updated emitChange type and body to use `interval_seconds`
  - Added backend contract test `test_photos_widget_config_uses_interval_seconds`
  - All 67 backend tests pass

Issue #2 - Clock config key mismatch: `use_24h` vs `format_24h` - DONE
  - Fixed ClockConfig.tsx to emit `format_24h` instead of `use_24h`
  - Renamed local state from `use24h`/`setUse24h` to `format24h`/`setFormat24h`
  - Updated handleChange type and body to use `format_24h`
  - Added backend contract test `test_clock_widget_config_uses_format_24h`
  - All 68 backend tests pass

Issue #3 - No Google OAuth token refresh - DONE (commit f2ce13c)
  - Added `get_valid_access_token()` in `google_auth.py` that checks `expires_at` and refreshes via `refresh_access_token()` when expired
  - Saves refreshed tokens (access_token, expires_at, preserves refresh_token) back to the Integration DB row
  - Returns None with warning log when no refresh_token is available
  - Updated `_get_google_access_token` in `refresh.py` to use `get_valid_access_token` (now async)
  - Updated `_get_access_token` in `google_data.py` to use `get_valid_access_token` (now async)
  - Updated `google_callback` in `integrations.py` to store `expires_at` during initial OAuth code exchange
  - Added 5 new tests in `test_token_refresh.py`: valid token, expired token refresh, no refresh token, no integration, missing expires_at
  - Updated `test_google_data.py` to mock `_get_access_token` directly instead of `load_or_create_key`
  - All 73 backend tests pass

Issue #4 - Calendar widget deduplication silently drops differing configs - DONE
  - Changed cache key from static `google_calendar` to parameterized `google_calendar_{sorted_ids}_{days_ahead}`
  - Updated `_collect_data_sources` in `refresh.py` to generate unique keys per calendar config
  - Updated `_get_cache_key` in `display.py` to match the new key format
  - Added test `test_collect_data_sources_calendar_different_configs` verifying two calendar widgets with different configs produce separate data source entries
  - All 74 backend tests pass

Issue #5 - Google Photos/Albums API pagination not handled - DONE
  - Added pagination loop in `fetch_albums` to follow `nextPageToken` via query params
  - Added pagination loop in `fetch_album_photos` to follow `nextPageToken` via request body
  - Both functions now accumulate results across all pages before returning
  - Added test `test_fetch_albums_paginates` verifying two pages of albums are combined
  - Added test `test_fetch_album_photos_paginates` verifying two pages of photos are combined
  - All 76 backend tests pass

Issue #6 - Theme settings not applied to dashboard display - DONE
  - Added theme CSS mapping constants in Dashboard.tsx for font_family, font_scale, and widget_background
  - Applied `text_color` (light/dark) as CSS color on root dashboard element
  - Applied `font_family` as CSS font-family on root dashboard element
  - Applied `font_scale` as CSS font-size (rem) on root dashboard element
  - Applied `widget_background` (transparent/semi-transparent/solid) as backgroundColor on each widget cell
  - No lint errors in Dashboard.tsx; all 76 backend tests pass

Issue #7 - `test_display_merges_cached_data` never asserts cached data - DONE (commit a2ed9c6)
  - Added assertion `weather_widget["data"] == {"temp": 72, "condition": "sunny"}` to verify cached data is actually merged
  - Test now fails if the display endpoint ignores the cache, previously it only checked widget_type
  - All 76 backend tests pass

Issue #8 - Path traversal in SPA fallback - DONE
  - Added `resolve().is_relative_to()` check in `serve_spa` route handler in `main.py`
  - After resolving the file path, verifies it stays within `frontend_dist` before serving
  - Paths that escape the dist directory fall through to serving `index.html`
  - Added test `test_path_traversal_with_encoded_slashes_blocked` (URL-encoded slash traversal)
  - Added test `test_path_traversal_with_encoded_dots_blocked` (URL-encoded dot traversal)
  - All 78 backend tests pass

Issue #9 - No auth on admin-only routes - DONE
  - Added `require_auth` as router-level dependency on `layouts.py`, `widgets.py`, and `google_data.py`
  - Added `require_auth` as per-endpoint dependency on `integrations.py` (list, connect, disconnect) — Google callback excluded since it's an OAuth redirect
  - Added `authed_client` fixture to `conftest.py` that sets up auth and logs in
  - Updated existing tests in `test_layouts.py`, `test_widgets.py`, `test_display.py`, `test_e2e.py`, `test_google_data.py` to use `authed_client`
  - Added 19 new tests in `test_admin_auth.py`: 401 for unauthenticated access to all admin endpoints, success with auth for layout/widget CRUD, display endpoint remains unauthenticated
  - All 97 backend tests pass

Issue #10 - Encryption key auto-creation with insecure permissions - DONE (commit 26f1a7d)
  - Added `os.chmod(path, 0o600)` after writing the key file in `load_or_create_key`
  - Key file now has owner-only read/write permissions instead of default umask (0644)
  - Added test `test_load_or_create_key_sets_restrictive_permissions` verifying file mode is `S_IRUSR | S_IWUSR`
  - All 98 backend tests pass

Issue #11 - Settings file written with default permissions - DONE
  - Added `os.chmod(path, 0o600)` after writing settings.json in `_save_settings` in `settings.py`
  - Settings file now has owner-only read/write permissions instead of default umask (0644)
  - Added test `test_settings_file_written_with_restricted_permissions` verifying file mode is `S_IRUSR | S_IWUSR`
  - All 99 backend tests pass

Issue #12 - Hardcoded secret key path bypasses Config - DONE (commit e126f94)
  - `refresh.py`: Changed `_get_google_access_token` to accept a `Config` parameter; reads `secret_key_path` and settings path from Config instead of hardcoding `/etc/wallboard/secret.key` and `~/.wallboard/settings.json`
  - Threaded `Config` through `_fetch_source`, `refresh_once`, and `start_refresh_loop`
  - `integrations.py`: Added `set_config()` / `_key_path()` / `_settings_path()` pattern (same as `settings.py`); removed hardcoded `KEY_PATH` and `SETTINGS_PATH` module constants
  - `google_data.py`: Added `set_config()` / `_key_path()` / `_settings_path()` pattern; removed hardcoded `KEY_PATH` and `SETTINGS_PATH` module constants
  - Updated `main.py` lifespan to call `integrations.set_config(config)` and `google_data.set_config(config)`
  - Updated `conftest.py` to set config on integrations and google_data routers in `tmp_config` fixture
  - Added 3 new tests in `test_hardcoded_paths.py`: refresh uses Config paths, integrations callback uses Config key path, google_data uses Config key path
  - All 102 backend tests pass

Issue #13 - Hardcoded settings path in integrations router - DONE (resolved as part of Issue #12)
  - Already fixed in Issue #12: `integrations.py` uses `set_config()` / `_settings_path()` pattern
  - No additional changes needed

Issue #14 - `display_refresh_interval` not wired to refresh loop or frontend - DONE
  - Backend: Added `refresh_interval` field to `DisplayResponse` schema (default 60)
  - Backend: Added `set_config()` / `_get_refresh_interval()` to `display.py` router; reads `display_refresh_interval` from settings.json
  - Backend: Added `_get_refresh_interval()` to `refresh.py`; refresh loop re-reads interval from settings on each cycle instead of using fixed startup value
  - Backend: Wired `display.set_config(config)` in `main.py` lifespan and `conftest.py` fixture
  - Frontend: Updated `DisplayResponse` type in `types.ts` to include `refresh_interval`
  - Frontend: Changed Dashboard.tsx from hardcoded `POLL_INTERVAL` to using `refresh_interval` from API response via recursive `setTimeout` pattern
  - Added test `test_display_includes_default_refresh_interval` verifying default 60s interval in response
  - Added test `test_display_includes_custom_refresh_interval` verifying configured interval is reflected
  - Added test `test_refresh_loop_rereads_interval_from_settings` verifying loop reads from settings file
  - Frontend builds cleanly with TypeScript; all 105 backend tests pass

Issue #15 - `wallboard update` CLI uses `npm install --production`, breaking the build - DONE (commit 09f1a1c)
  - Changed `npm install --production` to `npm ci` in the `cmd_update` function in `bin/wallboard`
  - `npm ci` installs all dependencies (including devDependencies like Vite, TypeScript, Tailwind) from lockfile
  - This ensures `npm run build` succeeds after dependency installation
  - All 105 backend tests pass

Issue #16 - Install script file ownership: git clone as root, no chown afterward - DONE (commit 88fd542)
  - Added `chown -R "$SERVICE_USER":"$SERVICE_USER" "$INSTALL_DIR"` after git clone/reset step (line 125)
  - Added `chown -R "$SERVICE_USER":"$SERVICE_USER" "$INSTALL_DIR"` after frontend build step (line 142)
  - First chown ensures all cloned/fetched repo files are owned by the wallboard user
  - Second chown ensures npm install/build artifacts (node_modules, dist/) are also owned correctly
  - Without this fix, the wallboard user cannot access files and `wallboard update` (git pull) fails with permission errors
  - All 105 backend tests pass

Issue #17 - Install script missing Alembic migration step - DONE (commit 97abdd1)
  - Added `alembic upgrade head` to install.sh after Python deps are installed, before frontend build
  - Runs as the wallboard service user with WALLBOARD_DB_PATH pointing to the correct database
  - Added `alembic upgrade head` to `cmd_update` in `bin/wallboard` after pip install, before frontend rebuild
  - Without this, Alembic migration tracking is never initialized and future schema changes cannot be applied
  - All 105 backend tests pass

Issue #18 - Display service `WantedBy=graphical.target` on headless Pi OS Lite - DONE (commit a51964f)
  - Changed `WantedBy=graphical.target` to `WantedBy=multi-user.target` in `system/wallboard-display.service`
  - Pi OS Lite boots to `multi-user.target`, not `graphical.target`, so the display service would never auto-start
  - Now matches the server service which already uses `multi-user.target`
  - All 105 backend tests pass

Issue #19 - Display service missing video/input group access - DONE (commit 7cac449)
  - Added `usermod -aG video,input wallboard` to install.sh after user creation
  - Added `SupplementaryGroups=video input` to wallboard-display.service for defense-in-depth
  - The wallboard user needs video and input group access for X server and Chromium kiosk mode
  - Without these groups, X server startup fails because it cannot access /dev/dri/* and /dev/input/*
  - All 105 backend tests pass

Issue #20 - `display.py` loads entire cache table on every poll - DONE
  - Changed `db.query(Cache).all()` to compute needed cache keys from the active layout's widgets first
  - Now uses `db.query(Cache).filter(Cache.source.in_(needed_keys)).all()` to only load relevant entries
  - Pre-computes cache keys per widget via `_get_cache_key`, collects unique keys, then does a single filtered query
  - When no widgets need cache data (e.g., all clock/notes widgets), skips the query entirely
  - Added test `test_display_only_loads_relevant_cache_entries` verifying correct data matching and `_get_cache_key` for all widget types
  - All 106 backend tests pass

Issue #21 - No `check_same_thread=False` for SQLite with FastAPI - DONE (commit bb9beb1)
  - Added `connect_args={"check_same_thread": False}` to `create_engine` in `database.py`
  - FastAPI runs sync route handlers in a threadpool; SQLite's default `check_same_thread=True` can raise `ProgrammingError` under concurrent load
  - This is the standard known-correct pattern for SQLite + FastAPI/Starlette
  - All 106 backend tests pass

Issue #22 - `activate_layout` race condition with ORM bulk update - DONE
  - Changed deactivation query to exclude the target layout: `Layout.id != layout_id`
  - Previously, bulk update set ALL active layouts to false (including the target), then ORM set the target back to true
  - This was fragile: depended on SQLAlchemy flush ordering to work correctly
  - Now the target layout is never deactivated, eliminating the race condition
  - Added test `test_activate_already_active_layout` verifying re-activating an already-active layout keeps it active and is the only active layout
  - All 107 backend tests pass

Issue #23 - `_resolve_weather_zip` mutates Pydantic model's config dict in-place - DONE
  - Changed `config.update(geo)` to `return {**config, **geo}` in `_resolve_weather_zip` in `widgets.py`
  - Previously, calling `config.update(geo)` mutated the dict that is an alias to `body.config` (the Pydantic model's internal dict)
  - Now returns a new merged dict without modifying the original, eliminating the mutation side-effect
  - Added test `test_resolve_weather_zip_does_not_mutate_input_config` that calls the function directly and asserts the original dict is unchanged
  - All 108 backend tests pass

Issue #24 - `test_db_session_injected` doesn't test what it claims - DONE (commit 35d31d9)
  - Changed test to use `authed_client` (layouts endpoint requires auth) and `GET /api/layouts` instead of `GET /api/health`
  - Test now inserts a layout via `db_session`, then verifies it appears in the API response via the client
  - Previously the test only hit `/api/health` which never queries the DB, so it passed even if sessions weren't shared
  - All 108 backend tests pass

Issue #25 - Encryption test silently swallows all exceptions - DONE
  - Replaced `try/except Exception: pass` with `pytest.raises(InvalidToken)` in `test_different_keys_cannot_decrypt`
  - Now imports `InvalidToken` from `cryptography.fernet` and asserts the specific exception type
  - Previously the test passed no matter what happened (including if decrypt succeeded with wrong key)
  - All 108 backend tests pass

Issue #26 - E2E test doesn't assert intermediate step responses - DONE (commit 0c1b0e5)
  - Added `assert resp.status_code == 201` after layout creation
  - Added `assert resp.status_code == 201` after each widget creation (3 widgets)
  - Added `assert resp.status_code == 200` after layout activation
  - Added `assert resp.status_code == 200` after batch position update
  - Previously, if any intermediate step failed with a validation error, the test only failed at the final assertion with a confusing error
  - All 108 backend tests pass

Issue #27 - Missing test coverage for key endpoints - DONE
  - Added `test_update_settings` in `test_settings.py`: verifies `PUT /api/settings` updates and persists values
  - Added `test_auth_setup_called_twice_returns_400` in `test_settings.py`: verifies duplicate setup is rejected
  - Added `test_logout_invalidates_session` in `test_settings.py`: verifies session cookie is invalidated after logout
  - Added `test_update_nonexistent_layout_returns_404` in `test_layouts.py`: verifies `PUT /api/layouts/999` returns 404
  - Added `test_delete_nonexistent_layout_returns_404` in `test_layouts.py`: verifies `DELETE /api/layouts/999` returns 404
  - Added `test_update_nonexistent_widget_returns_404` in `test_widgets.py`: verifies `PUT /api/widgets/999` returns 404
  - Added `test_delete_nonexistent_widget_returns_404` in `test_widgets.py`: verifies `DELETE /api/widgets/999` returns 404
  - Added `test_batch_positions_nonexistent_layout_returns_404` in `test_widgets.py`: verifies batch update with bad layout ID returns 404
  - Added `test_batch_positions_wrong_widget_id_returns_404` in `test_widgets.py`: verifies batch update with wrong widget ID returns 404
  - All 117 backend tests pass

Issue #28 - `tmp_config` fixture doesn't reset `_config` on teardown - DONE
  - Added `_config = None` reset for all four routers (settings, display, integrations, google_data) in `tmp_config` fixture teardown in `conftest.py`
  - Previously only `_sessions.clear()` was called; `_config` from one test leaked to subsequent tests that don't use the fixture
  - Added `test_fixture_teardown.py` with two tests: one that uses `tmp_config` and verifies config is set, and one that runs after and verifies `_config` is reset to None
  - All 119 backend tests pass

Issue #29 - Frontend `Layout` type doesn't match `LayoutListResponse` - DONE (commit fed7192)
  - Added `LayoutListItem` interface to `types.ts` with `widget_count: number` instead of `widgets: Widget[]`
  - Updated `api.getLayouts()` in `api.ts` to return `LayoutListItem[]` instead of `Layout[]`
  - Updated `Layouts.tsx` to use `LayoutListItem` type and access `layout.widget_count` directly
  - Removed `as any` cast and fallback chain `(layout as any).widget_count ?? layout.widgets?.length ?? 0`
  - Frontend builds cleanly with TypeScript; all 119 backend tests pass

Issue #30 - `WidgetConfig` useEffect resets edits on parent re-render - DONE (commit 317a973)
  - Changed useEffect dependency from `widget.config` (object reference) to `JSON.stringify(widget.config)` (stable string)
  - Memoize config JSON outside the effect; parse it inside to set state only when content actually changes
  - Previously, every parent re-render created a new object reference for `widget.config`, triggering the effect and resetting `dirty` to false, wiping unsaved edits
  - Frontend builds cleanly with TypeScript; all 119 backend tests pass

Issue #31 - `AdminShell` auth check conflates network errors with 401 - DONE
  - Added `error` state to `AuthState` union type in `AdminShell.tsx`
  - In the catch block of `checkAuth`, now checks if the error message contains "401" to distinguish auth failures from other errors
  - 401 errors show the login screen as before; other errors (network failures, server errors) show an error state with a retry button
  - Error state displays "Unable to connect to the server" with the error message and a retry button
  - Previously, any error (including network timeouts) incorrectly showed the login form
  - Frontend builds cleanly with TypeScript; all 119 backend tests pass
